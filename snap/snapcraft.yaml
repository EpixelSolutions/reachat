name: reachatdesktop # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '1.0.0' # just for humans, typically '1.2+git' or '1.3.2'
summary: Official Linux Desktop app for Reach@ by Epixel solutions # 79 char long summary
description: |
  Official Linux Desktop app for Reach@ by Epixel solutions. 
  Reach@ is the official chat app by epixel solutions

website: chat.epixel.in 
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
